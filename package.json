{
  "name": "good-dog",
  "version": "1.0.0",
  "description": "Dog bark detector with continuous A/V recording and event review UI",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --forceExit --testTimeout=10000",
    "ngrok": "node scripts/ngrok.js"
  },
  "dependencies": {
    "@ngrok/ngrok": "^1.7.0",
    "ffmpeg-static": "^5.3.0",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "transform": {}
  }
}
